# Exclude version control
.git
.gitignore

# Exclude development files
node_modules
npm-debug.log
.env
.env.*
.dockerignore
Dockerfile*
docker-compose*
.docker

# Exclude IDE files
.vscode
.idea

# Exclude client directory for backend build
client/

# Keep WhatsApp sessions folder but exclude its contents
sessions/*
!sessions/.gitkeep

# System files
.DS_Store
Thumbs.db