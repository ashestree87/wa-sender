# Exclude version control
.git
.gitignore

# Exclude development files
node_modules
npm-debug.log
.env
.env.*
.dockerignore
Dockerfile*
docker-compose*
.docker

# Exclude IDE files
.vscode
.idea

# Keep WhatsApp sessions folder but exclude its contents
sessions/*
!sessions/.gitkeep

# System files
.DS_Store
Thumbs.db

# Only exclude source files, not the build
client/node_modules
# Do NOT exclude client/build